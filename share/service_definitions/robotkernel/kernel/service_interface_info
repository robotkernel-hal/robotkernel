services:
  robotkernel/kernel/service_interface_info:
    request:
      name: { type: string }
    response:
      owner: { type: string }
      error_message: { type: string, default: "no error" }
