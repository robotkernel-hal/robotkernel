services:
  robotkernel/kernel/del_pd_injection:
    request:
      pd_dev: { type: string }
      field_name: { type: string }
    response:
      error_message: { type: string, default: "no error" }
