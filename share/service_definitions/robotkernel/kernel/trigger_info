services:
  - name: robotkernel/kernel/trigger_info
    request:
      - { name: name, type: string }
    response:
      - { name: owner, type: string }
      - { name: rate, type: double }
      - { name: error_message, type: string, default: "no error" }
