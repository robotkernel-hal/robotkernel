services:
  - name: robotkernel/kernel/trigger_info
    request:
      name: { type: string }
    response:
      owner: { type: string }
      rate: { type: double }
      error_message: { type: string, default: "no error" }
