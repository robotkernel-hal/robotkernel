name: C/C++ CI

on:
  push:
    branches: [ "release/6.0.0" ]
  pull_request:
    branches: [ "release/6.0.0" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: autreconf
      run: autoreconf -if
    - name: configure
      run: ./configure
    - name: make
      run: make
    - name: make check
      run: make check
    - name: make distcheck
      run: make distcheck
